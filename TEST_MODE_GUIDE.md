# 테스트 모드 사용 가이드

ADHD 자가진단 테스트를 실제 데이터 수집 없이 테스트할 수 있는 기능입니다.

## 🧪 테스트 모드란?

테스트 모드는 개발자나 관리자가 웹사이트를 테스트할 때 **Google Sheets에 데이터가 저장되지 않도록** 하는 기능입니다. 테스트 모드로 접속하면:

✅ 모든 기능이 정상적으로 작동
✅ 결과 페이지 정상 표시
✅ Google Sheets에 데이터 저장 **안 됨**
✅ 화면 상단에 테스트 모드 배너 표시

---

## 🚀 사용 방법

### 방법 1: `?test=true` 파라미터 사용

URL 뒤에 `?test=true`를 추가하세요:

```
http://localhost:8000/index.html?test=true
```

또는 실제 도메인:

```
https://yourdomain.com/index.html?test=true
```

### 방법 2: `?debug=true` 파라미터 사용

`?debug=true`도 동일하게 작동합니다:

```
http://localhost:8000/index.html?debug=true
```

---

## 📺 테스트 모드 확인 방법

### 1. 시각적 확인

테스트 모드로 접속하면 화면 **상단에 빨간색 배너**가 표시됩니다:

```
🧪 테스트 모드 활성화 - 데이터가 엑셀 시트에 저장되지 않습니다
```

### 2. 콘솔 확인

브라우저 개발자 도구 (F12)의 콘솔 탭에서 다음 메시지를 확인할 수 있습니다:

**페이지 로드 시:**
```
🧪 테스트 모드 활성화
데이터가 Google Sheets에 저장되지 않습니다.
```

**결과 제출 시:**
```
🧪 테스트 모드: Google Sheets 제출 건너뛰기
제출될 예정이었던 데이터: { ... }
```

---

## ✅ 테스트 시나리오

### 시나리오 1: 기본 기능 테스트

```bash
# 1. 테스트 모드로 접속
http://localhost:8000/index.html?test=true

# 2. 테스트 진행
- 성별 선택
- 12개 질문에 답변
- 결과 확인

# 3. Google Sheets 확인
→ 데이터가 추가되지 않음 ✅
```

### 시나리오 2: 설문조사 CTA 버전 테스트

```bash
# 1. 테스트 모드 + 고위험군 결과
http://localhost:8000/index.html?test=true

# 2. 9개 이상 "그렇다" 선택
→ 고위험군 결과 + 설문조사 버튼 표시

# 3. 설문조사 버튼 테스트
→ 클릭 시 설문 페이지로 이동
→ Google Sheets에 테스트 데이터 저장 안 됨 ✅
```

### 시나리오 3: UTM 파라미터 + 테스트 모드

```bash
# 테스트 모드와 UTM 파라미터 동시 사용 가능
http://localhost:8000/index.html?test=true&utm_source=youtube&utm_medium=video

# UTM 추적은 정상 작동하지만 데이터는 저장 안 됨
```

---

## 🔍 개발자 도구 확인

### 콘솔에서 확인할 수 있는 정보

1. **테스트 모드 활성화 상태**
```javascript
🧪 테스트 모드 활성화
```

2. **제출될 예정이었던 데이터 (미리보기)**
```javascript
제출될 예정이었던 데이터: {
  gender: "skip",
  answers: [true, true, false, ...],
  score: 9,
  utm_source: "youtube",
  utm_medium: "video",
  utm_campaign: "test"
}
```

이를 통해 어떤 데이터가 Google Sheets에 저장될지 미리 확인할 수 있습니다.

---

## ⚠️ 주의사항

### 1. 일반 사용자에게 테스트 URL 공유 금지

테스트 모드 URL을 일반 사용자에게 공유하면 실제 데이터가 수집되지 않습니다!

❌ **잘못된 예:**
```
https://yourdomain.com/index.html?test=true
↑ 이 URL을 SNS에 공유하면 모든 데이터가 수집 안 됨!
```

✅ **올바른 예:**
```
https://yourdomain.com/index.html
↑ 일반 사용자에게는 파라미터 없는 URL 공유
```

### 2. 테스트 모드는 개발/관리 목적으로만 사용

- ✅ 기능 테스트
- ✅ 디자인 확인
- ✅ 버전 비교 (A/B 테스트 사전 검토)
- ❌ 실제 사용자 데이터 수집

### 3. 배포 전 확인 사항

배포 전에 **일반 모드**(파라미터 없음)로 테스트하여 데이터 수집이 정상 작동하는지 확인하세요:

```bash
# 최종 배포 전 테스트
http://localhost:8000/index.html

# 결과 확인
→ Google Sheets에 데이터 정상 저장됨 ✅
```

---

## 🛠️ 고급 사용법

### 여러 파라미터 조합

```bash
# 테스트 모드 + UTM 파라미터
http://localhost:8000/index.html?test=true&utm_source=instagram&utm_medium=story

# debug 파라미터 사용 (test와 동일)
http://localhost:8000/index.html?debug=true

# 실수로 대문자 사용 시 작동 안 함 (주의!)
http://localhost:8000/index.html?test=True  ❌
http://localhost:8000/index.html?TEST=true  ❌
```

### 북마크 저장

자주 사용하는 테스트 URL을 브라우저 북마크에 저장하세요:

```
이름: ADHD 테스트 (테스트 모드)
URL: http://localhost:8000/index.html?test=true
```

---

## 🐛 트러블슈팅

### 문제 1: 테스트 모드 배너가 보이지 않음

**원인:**
- URL 파라미터 오타
- 소문자로 입력하지 않음

**해결:**
```bash
# 잘못된 예
?Test=true  ❌
?test=True  ❌
?TEST=true  ❌

# 올바른 예
?test=true  ✅
?debug=true ✅
```

### 문제 2: Google Sheets에 데이터가 여전히 저장됨

**확인 사항:**
1. 테스트 모드 배너가 표시되는지 확인
2. F12 > 콘솔에서 "🧪 테스트 모드 활성화" 메시지 확인
3. 캐시 삭제 후 재시도 (Cmd+Shift+R / Ctrl+Shift+R)

### 문제 3: 테스트 모드인데 정상 작동 안 함

**확인 사항:**
1. 브라우저 콘솔에 JavaScript 오류가 있는지 확인
2. `app.js` 파일이 제대로 로드되었는지 확인
3. 브라우저 캐시 삭제 후 재시도

---

## 📊 테스트 체크리스트

테스트 시 다음 항목을 확인하세요:

### 기본 기능
- [ ] 테스트 모드 배너 표시됨
- [ ] 랜딩 페이지 정상 표시
- [ ] 성별 선택 페이지 작동
- [ ] 12개 질문 정상 표시
- [ ] 진행률 바 업데이트됨
- [ ] 결과 페이지 정상 표시
- [ ] 콘솔에 "Google Sheets 제출 건너뛰기" 메시지 표시
- [ ] Google Sheets에 데이터 저장 안 됨 ✅

### 고위험군 기능 (9-12점)
- [ ] 설문조사 버튼 표시됨
- [ ] 버튼 클릭 시 설문 페이지로 이동
- [ ] CTA 버전 전환 정상 작동

### UTM 추적
- [ ] UTM 파라미터 정상 인식
- [ ] 콘솔에 유입 경로 정보 표시
- [ ] 테스트 모드에서도 추적 데이터 미리보기 가능

---

## 🎓 실전 예제

### 예제 1: 로컬 개발 환경에서 테스트

```bash
# 1. 로컬 서버 실행
python3 -m http.server 8000

# 2. 브라우저에서 테스트 모드로 접속
http://localhost:8000/index.html?test=true

# 3. 테스트 진행
- 성별 선택
- 9개 "그렇다" 선택 (고위험군)
- 결과 확인

# 4. 콘솔 확인
F12 > Console 탭에서 메시지 확인
```

### 예제 2: 설문조사 CTA 버전 비교

```bash
# app.js 287-308 라인에서 버전 변경
# 버전 D → 버전 B로 변경

# 테스트 모드로 재접속
http://localhost:8000/index.html?test=true

# 결과 비교
→ 데이터 저장 없이 UI만 확인 가능
```

### 예제 3: 실제 도메인 배포 후 테스트

```bash
# 배포 후 테스트 모드로 먼저 확인
https://yourdomain.com/index.html?test=true

# 문제 없으면 일반 모드로 최종 확인
https://yourdomain.com/index.html

# Google Sheets에서 데이터 수집 확인
→ 일반 모드 테스트 결과만 저장됨 ✅
```

---

## 📖 요약

| 항목 | 내용 |
|------|------|
| **활성화 방법** | URL에 `?test=true` 또는 `?debug=true` 추가 |
| **시각적 표시** | 화면 상단 빨간색 배너 |
| **콘솔 메시지** | "🧪 테스트 모드 활성화" |
| **데이터 저장** | Google Sheets에 저장 안 됨 |
| **기능 작동** | 모든 기능 정상 작동 |
| **사용 목적** | 개발/테스트 전용 |

---

## 🔗 관련 문서

- `SURVEY_CTA_VERSIONS.md` - 설문조사 CTA 버전 비교
- `UTM_URL_GUIDE.md` - UTM 파라미터 가이드
- `GOOGLE_SHEETS_SETUP.md` - Google Sheets 연동 설정

---

## 📞 지원

테스트 모드 관련 문제가 발생하면:

1. 브라우저 콘솔 (F12) 확인
2. 캐시 삭제 후 재시도
3. 다른 브라우저에서 테스트
4. `app.js` 파일의 `checkTestMode()` 함수 확인

---

**마지막 업데이트:** 2025-11-04
**버전:** 1.0
